export enum GENDER {
    Male = 'Male',
    Female = 'Female',
}

export enum JOB_LEVEL {
    Manager = "Manager",
    Executive = "Executive",
    SeniorManager = "Senior Manager"
}

export enum DATA_TYPE {
    Employees = "employees",
    Accounts = "accounts"
}